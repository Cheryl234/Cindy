#Introduction of pip

  pip -V #the version of pip
  pip install requests #install the package 'requests' into python
  pip uninstall requests #uninstall the package
  pip list #list installed packages
  pip freeze #the packages installed by pip
  pip install --upgrate pip #update pip or anyother packages.

#Two common ways for installing packages by pip

  ##Install a package

  pip install package_name 
  pip install package_name -i website #website is the mirror website and you can download faster
  
  ##Install multiple packages using a requirements file
  pip install r- requirements.txt #it contains the package names and versions, eg.requests ==  2.32.3


  


